import * as React from "react";
import * as ReactDOM from "react-dom";

const App: React.FC = () => {
    return <h1>React With Force!</h1>;
};

ReactDOM.render(
    <App />,
    document.getElementById("app"),
);
